// cypress/e2e/base_rate_history.cy.js
/* global describe, it */

describe('Base rate history dialog', () => {
  it('adds a new row with effectDate and shows editedBy tooltip', () => {
    // Placeholder test; would open dialog, add entry, and verify
  })
})
